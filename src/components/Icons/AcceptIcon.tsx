import React, {FC} from 'react';
import accept from '../../assets/icons/check.svg'

const AcceptIcon : FC = (props) => {
    return (
        <img src={accept} alt=""/>
    );
}

export default AcceptIcon;
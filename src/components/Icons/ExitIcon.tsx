import React, {FC} from 'react';
import exit from '../../assets/icons/x-circle.svg'

const ExitIcon : FC = (props) => {
    return (
        <img src={exit} alt=""/>
    );
}

export default ExitIcon;